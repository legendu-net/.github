name: Self Remove Branches

on:
  schedule:
    - cron: '0 3 * * *'
      timezone: 'America/Los_Angeles'

jobs:
  remove_branch:
    uses: ./.github/workflows/remove-branch.yaml
    secrets: inherit
